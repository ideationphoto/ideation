# CLAUDE.md

이 문서는 본 프로젝트에서 Claude(또는 다른 AI 코딩 어시스턴트)가 일관된 방향으로 작업하도록 돕기 위한 가이드입니다. 코드 작성, 리뷰, 리팩토링 시 이 문서의 원칙과 규칙을 따라주세요.

## 1. 프로젝트 개요

### 1.1 한 줄 요약
사용자가 풍경(인물 없는 배경)을 먼저 촬영하면, AI가 그 풍경에 가장 잘 어울리는 인물 위치 3곳을 실루엣으로 추천하고, 사용자가 그중 하나를 선택하면 카메라 화면에 반투명 가이드로 오버레이해 누구나 쉽게 좋은 구도의 인물 사진을 찍을 수 있게 해주는 모바일 앱.

### 1.2 핵심 사용자 플로우
1. 사용자가 앱에서 풍경을 촬영
2. 서버가 이미지를 분석해 후보 위치마다 실루엣을 합성하고, NIMA 모델로 미적 점수를 매겨 상위 3개 추출
3. 각 후보 위치에 인물 실루엣을 합성한 미리보기 3장을 사용자에게 제시
4. 사용자가 1개를 선택
5. 카메라 화면에 풍경(반투명) + 선택된 실루엣을 오버레이로 표시
6. 사용자가 가이드에 맞춰 인물 사진 촬영

### 1.3 기술 스택
- **프론트엔드(모바일)**: React Native (TypeScript)
- **백엔드(분석 서버)**: Python + FastAPI
- **AI/CV 라이브러리**: PyTorch, OpenCV, Pillow, NumPy
- **추천 알고리즘 방식**: **NIMA (Neural Image Assessment) 기반 미적 점수화**
  - 구글이 공개한 사전학습 모델을 그대로 사용
  - 학습 데이터 수집/모델 학습 없음
  - 깊이 추정·세그멘테이션은 후보 필터링과 실루엣 크기 계산용
- **도커컴포즈사용**

## 2. 아키텍처

### 2.1 전체 구조
```
[React Native App]
   ├── 카메라 모듈 (촬영, 오버레이)
   ├── API 클라이언트
   └── 화면 상태 관리 (촬영 → 분석 → 선택 → 가이드)
        ↕  HTTPS / multipart-form
[FastAPI Server]
   ├── /analyze : 풍경 → 후보 3개 + 실루엣 합성 미리보기
   └── 분석 파이프라인
         ├─ 전처리 (리사이즈, 정규화)
         ├─ 깊이 추정 (MiDaS)         ← 사람 크기 계산용
         ├─ 세그멘테이션               ← 인물 못 서는 영역 제외용
         ├─ 후보 격자 생성 + 무효 위치 제거
         ├─ 각 후보에 실루엣 합성
         ├─ NIMA로 합성 이미지 채점     ← 핵심 결정자
         └─ NMS로 다양성 있는 상위 3개 선정
```

### 2.2 디렉토리 구조
```
project-root/
├── CLAUDE.md
├── README.md
├── app/                       # React Native 앱
│   ├── src/
│   │   ├── screens/           # CameraScreen, PreviewScreen, GuideScreen
│   │   ├── components/        # SilhouetteOverlay, CandidateCard 등
│   │   ├── api/               # FastAPI 클라이언트
│   │   ├── hooks/
│   │   ├── store/             # 상태 관리
│   │   └── utils/
│   ├── assets/
│   │   └── silhouettes/       # 포즈별 실루엣 PNG
│   └── package.json
└── server/                    # FastAPI 서버
    ├── app/
    │   ├── main.py
    │   ├── routers/
    │   │   └── analyze.py
    │   ├── pipeline/
    │   │   ├── preprocess.py
    │   │   ├── depth.py            # MiDaS 깊이 추정
    │   │   ├── segmentation.py
    │   │   ├── candidates.py       # 격자 생성 + 무효 위치 제거
    │   │   ├── nima.py             # NIMA 모델 래퍼 (핵심)
    │   │   └── nms.py
    │   ├── compositor/
    │   │   └── silhouette.py       # 실루엣 합성, 크기 계산
    │   ├── schemas/                # Pydantic 모델
    │   └── config.py
    ├── models/                     # 사전학습 모델 파일 (.pt)
    │   ├── nima.pt
    │   └── midas.pt
    ├── assets/
    │   └── silhouettes/
    ├── requirements.txt
    └── pyproject.toml
```

## 3. 추천 알고리즘 (NIMA 기반)

### 3.1 NIMA란
- 구글이 2018년 공개한 사진 미적 평가 CNN 모델
- 입력: 사진 1장
- 출력: 1~10점 미적 점수 분포 (보통 평균값을 점수로 사용)
- AVA Dataset(사진 25만 장 + 사람의 미적 평가) 기반 사전학습
- MobileNet 백본 기준 약 17MB
- 본 프로젝트에서는 학습된 가중치를 그대로 사용 (재학습/파인튜닝 안 함)

### 3.2 입력
- 사용자가 촬영한 풍경 이미지 1장 (인물 없음)
- 메타데이터(가능 시): 가로/세로, EXIF 방향

### 3.3 파이프라인 단계

**1) 전처리**
- 입력 이미지를 한 변 1024px 이하로 리사이즈 (속도 확보)
- EXIF 회전 보정

**2) 후보 격자 생성**
- 이미지를 격자(예: 8×8 = 64개)로 샘플링
- 각 격자점이 인물의 발 위치 후보

**3) 사전 필터링 (가벼운 분석)**
- **세그멘테이션**으로 무효 영역 제외: 하늘, 물 표면, 나무 줄기, 건물 벽 등
- 너무 가장자리에 붙은 후보 제외
- 결과: 보통 64개 중 15~25개의 유효 후보가 남음

**4) 실루엣 크기 계산 (깊이 기반)**
- MiDaS 깊이 맵에서 각 후보 위치의 깊이값 `d` 읽기
- 공식으로 실루엣 픽셀 높이 계산:
  ```
  silhouette_height_ratio = clamp(0.6 / (d + 0.1), 0.1, 0.85)
  silhouette_pixel_height = image_height * silhouette_height_ratio
  ```
- 가까우면 크게, 멀면 작게

**5) 합성**
- 각 유효 후보마다:
  - 실루엣 PNG를 계산된 크기로 리사이즈
  - 실루엣의 발이 후보 좌표에 닿도록 정렬
  - 풍경 위에 합성 → 가상 합성 이미지 1장
- 결과: 후보 수만큼의 합성 이미지 (예: 20장)

**6) NIMA 채점 (핵심)**
- 합성 이미지를 모두 NIMA에 통과
- 각 합성 이미지가 미적 점수(1~10) 1개씩 받음
- 점수가 높을수록 "사람들이 좋다고 느낄 사진"

**7) NMS로 상위 3개 선정**
- 점수 내림차순 정렬
- 1등 채택 → 일정 반경(이미지 너비의 15%) 내 후보는 제외
- 남은 후보 중 1등 채택 → 또 반경 제외
- 반복으로 서로 다른 구도 3개 확보

**8) 응답**
- 3개 후보의 위치/점수/합성 미리보기를 클라이언트에 반환

### 3.4 NIMA가 결정하지 않는 것 (우리가 직접 해야 하는 일)
- **후보 위치 생성**: 격자 + 세그멘테이션
- **인물 크기**: 깊이 맵 + 비례 공식
- **합성**: PIL/OpenCV로 실루엣 붙이기
- **다양성 보장**: NMS

NIMA는 마지막 채점만 한다. 합성된 사진을 던지면 점수만 돌려받는 채점관 역할.

## 4. API 명세

### 4.1 POST `/analyze`
**요청**: `multipart/form-data`
- `image`: 풍경 이미지 파일 (JPEG/PNG)
- `pose` (optional): 실루엣 포즈 ID, 기본값 `"standing"`

**응답** (`200 OK`, JSON):
```json
{
  "request_id": "uuid",
  "candidates": [
    {
      "rank": 1,
      "nima_score": 7.42,
      "position": { "x": 0.33, "y": 0.66 },
      "scale": 0.42,
      "preview_base64": "data:image/png;base64,iVBORw0KG..."
    },
    { "rank": 2, "...": "..." },
    { "rank": 3, "...": "..." }
  ]
}
```

미리보기는 응답에 base64로 직접 박는다(별도 저장소 불필요).

**오류 응답**:
- `400`: 이미지 형식 오류
- `413`: 이미지 크기 초과
- `500`: 분석 실패

## 5. 코딩 규칙

### 5.1 공통
- 영어 주석 기본, 핵심 로직(특히 합성·크기 계산)은 한국어 주석 허용
- 매직 넘버 금지: 격자 크기, NMS 반경, 실루엣 크기 공식 상수 등은 `config.py`로 분리
- 모든 외부 입력은 검증 후 사용

### 5.2 React Native (TypeScript)
- 함수형 컴포넌트 + Hooks
- 카메라: `react-native-vision-camera`
- 이미지 오버레이: `react-native-svg` 또는 `Skia`
- ESLint + Prettier
- 컴포넌트 단일 책임, 화면 단위는 `screens/`, 재사용 단위는 `components/`

### 5.3 FastAPI (Python)
- Python 3.11+
- 타입 힌트 필수, Pydantic v2로 요청/응답 정의
- 모델은 서버 시작 시 한 번만 로드해서 메모리에 상주 (요청마다 재로드 금지)
- 무거운 추론은 `run_in_executor` 또는 별도 워커로
- Black + Ruff

### 5.4 모델 관리
- 모델 파일은 `server/models/` 디렉토리에 정적 파일로 저장
- 서버 시작 시 메모리 로드, 그 이후 디스크 접근 없음
- 모델 버전은 파일명에 명시 (`nima_v1.pt`, `nima_v2.pt`)

## 6. 인프라
- 서버 1대 (FastAPI 단일 인스턴스)
- DB 없음, S3 없음, Redis 없음
- 모델 파일은 서버 디스크에 정적으로 둠
- 사용자 이미지는 메모리에서만 처리, 응답 후 즉시 폐기

## 7. AI 어시스턴트(Claude)에게 거는 작업 가이드

### 7.1 코드 작성 시
- NIMA 호출 부분을 건드릴 때는 **모델 입력 형식(정규화, 리사이즈)** 을 항상 사전학습 시점과 동일하게 맞출 것
- 새 의존성 추가 시 `requirements.txt`에 이유 코멘트
- 추가 모델(MiDaS 외)을 도입하려면 응답 시간 영향을 추정해 보고

### 7.2 하지 말아야 할 것
- NIMA 재학습/파인튜닝 코드 추가 금지 (사전학습 가중치 그대로 사용이 원칙)
- DB·S3·외부 저장소 도입 금지 (현재 단계에서 불필요)
- 사용자 이미지를 디스크에 쓰는 코드 금지 (메모리 처리만)
- 매직 넘버 하드코딩 금지, `config.py`로

### 7.3 모호할 때
- 알고리즘 선택지가 여러 개라면 **단순성 → 응답 속도 → 결과 품질** 순으로
- 결정이 필요한 사항은 코드로 결론 내리지 말고 PR 설명에 옵션을 정리해 질문

---
*이 문서는 살아있는 문서입니다. 파이프라인이나 모델 구성이 바뀌면 함께 업데이트하세요.*
